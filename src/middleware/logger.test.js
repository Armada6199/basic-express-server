'use strict'
const logger =require('./logger')
describe('testing the logger ',()=>{
    let consoleSpy;
    const req={};
    const res={};
    const next=jest.fn();
    it('see the logged ',()=>{
        logger
    })
})